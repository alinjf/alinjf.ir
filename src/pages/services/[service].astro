---
import BlogPost from "../../components/BlogPost.astro";

export async function getStaticPaths() {
    const posts = await Astro.glob("../../posts/services/*.md");

    return posts.map((post) => ({
        params: {
            service: post.frontmatter.url,
        },
        props: {
            post,
        },
    }));
}

const { Content, frontmatter } = Astro.props.post;
console.log(frontmatter);
---

<BlogPost title={frontmatter.pageTitle} description={frontmatter.pageDescription}>
    <Content />
</BlogPost>
